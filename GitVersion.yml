next-version: 2.0
mode: ContinuousDelivery
next-version: 2.0
branches:
  develop:
    regex: dev(elop)?(ment)?(_v2)?$
    tag: beta
  feature:
    regex: features?[/-]
    tag: alpha
    source-branches: ['master', 'develop', 'feature', 'hotfix', 'support']
  release:
    regex: releases?[/-]
    tag: rc
    source-branches: ['hotfix', 'develop', 'master', 'feature', 'support']
ignore:
  sha: []
