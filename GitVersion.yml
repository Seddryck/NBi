mode: ContinuousDelivery
assembly-informational-format: '{MajorMinorPatch}-{PreReleaseLabel}.{BuildMetaData} ({BranchName} // {PreReleaseNumber})'
branches:
  dev(elop)?(ment)?$:
    tag: alpha
  features?[/-]:
    tag: ci
ignore:
  sha: []
