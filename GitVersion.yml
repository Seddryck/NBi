mode: ContinuousDelivery
assembly-informational-format: '{MajorMinorPatch}-{PreReleaseLabel}.{CommitsSinceVersionSource} ({BranchName} // {PreReleaseNumber})'
branches:
  dev(elop)?(ment)?$:
    tag: alpha
  features?[/-]:
    tag: ci
ignore:
  sha: []
